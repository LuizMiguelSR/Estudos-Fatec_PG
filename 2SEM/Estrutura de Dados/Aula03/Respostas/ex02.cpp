#include <stdio.h>
#include <iostream>
#include <locale.h>
using namespace std;
int main () {
    setlocale(LC_ALL,"");
    system("cls");

    int vetor[5], som, i=0;

    for (i=0; i < 5; i++) {
        cout<<"Digite um número: ";
        cin>>vetor[i];
        if (i == 0) {
            break;
        } else {

        }
    }

    return 0;
}